name: eatwise
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '3.5.4'
dependencies:
  flutter:
    sdk: flutter
  iconsax: ^0.0.8 
  image_picker: ^1.0.7

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
      - assets/images/
